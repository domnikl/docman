const React = require("react");

import Main from "./components/Main";

export class App extends React.Component {
  constructor(props) {
    super(props);
  }

  render() {
    return <Main />;
  }
}
